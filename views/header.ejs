<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>NETSLIP</title>
</head>

<body>

    <div class="container">
        <h1 class="my-4 text-center">NETSLIP</h1>
    </div>

    <nav class="navbar navbar-expand-lg bg-primary-subtle p-4">
        <div class="container-fluid">
            <a class="navbar-brand" aria-current="page" href="/">Accueil</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <% if (userToken) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/favorite/get-favorites">Mes Favoris</a>
                        </li>
                    <% } %>
                </ul>
                <ul class="navbar-nav ms-auto">
                    <% if (userToken) { %>
                        <li class="nav-item">
                            <form action="/auth/logout" method="get">
                                <button class="btn btn-primary" type="submit">Déconnexion</button>
                            </form>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="btn btn-primary" href="/auth">Se connecter</a>
                        </li>
                    <% } %>
                    <% if (userToken) { %>
                        <li class="nav-item mx-4">
                            <p>Bienvenue, <%= pseudo %> !</p>
                        </li>
                    <% } %>
                    <% if (roles && roles.includes('Admin')) { %>
                        <li class="nav-item">
                            <a class="btn btn-danger" href="/admin">Admin Panel</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">

        <a class="navbar-brand" aria-current="page" href="/">Accueil
        </a>
        
        <% if (userToken && userEmail) { %>
        <a class="nav-link" href="/get-favorites">Mes favoris</a>
        <% } %>
        <% if (userToken && userEmail) { %>
            <form  action="/user/logout" method="get">
                <button type="submit">Déconnexion</button>
            </form>
        <% } else { %>
            <a class="nav-link" href="/auth">Se connecter</a>
        <% } %>
        
        </div>
      </nav>
      <% if (userToken && userEmail) { %>
        <p>Bienvenue, <%= userEmail %>!</p>
    <% } %> -->